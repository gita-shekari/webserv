# Webserv

Initial project structure for the 42 Webserv project.

## First step

The project currently contains only the initial structure and the first two classes:

- `Server`
- `Config`

No server or HTTP functionality is implemented yet.
